<Window x:Class="PictureSet.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PictureSet"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Loaded="Window_Loaded" SizeChanged="Window_SizeChanged">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel x:Name="ToolPanel" Grid.Row="0" Orientation="Horizontal">
            <Button x:Name="button_Load" Content="Load" Width="64" Click="button_Load_Click"/>
            <Button x:Name="button_Save" Content="Save" Width="64" Click="button_Save_Click"/>
            <Button x:Name="button_AddPicture" Content="Add picture" Width="86" Click="button_AddPicture_Click"/>
            <Button x:Name="button_SetBasePath" Content="Set base path" Width="128" Click="button_SetBasePath_Click"/>

        </StackPanel>
        <Canvas x:Name="pictureCanvas" Grid.Row="1" AllowDrop="True" Drop="PictureCanvas_Drop" Focusable="True" ScrollViewer.HorizontalScrollBarVisibility="Visible" ScrollViewer.VerticalScrollBarVisibility="Visible">

        </Canvas>
    </Grid>
</Window>
